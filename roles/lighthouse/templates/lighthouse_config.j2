server {
    listen 80;
    server_name localhost;

    root {{ lighthouse_location_dir }};
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    error_page 404 /404.html; 

    location ~* \.(html|htm|jpg|jpeg|png|gif|css|js)$ {
        allow all;
    }
}
